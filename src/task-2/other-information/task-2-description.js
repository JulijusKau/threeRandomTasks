/**
 * Instructions:
 *
 * You are given the example of users list.
 * Use Your React knowledge to achieve similar result.
 *
 * Requirements:
 *
 * 1. Use HTTP request to fetch users from https://jsonplaceholder.typicode.com/users
 * 2. Print only given amount of users in the list (amount can be defined).
 * 3. Delete button appears only on hover and removes item from UI permanently.
 * 4. Show more button would append more items to the list.
 * 5. Amount to be added can be set through parameter.
 */
